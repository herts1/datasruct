# Root-level canonical test entry for the whole problem repository.
# Usage:  mingw32-make test    (runs run-all-tests.ps1 in every problem subdir)

test:
	powershell -NoProfile -ExecutionPolicy Bypass -File run-all-tests.ps1

.PHONY: test
